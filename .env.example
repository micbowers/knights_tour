# Used by the Vercel function /api/interpret. Set in Vercel's project
# environment variables for production; set here only if you run vercel dev.
ANTHROPIC_API_KEY=sk-ant-...

# Used by tools/generate_alien_voices.py only (local content generation).
OPENAI_API_KEY=sk-...
